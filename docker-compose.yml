version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: my-cloud
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: my-cloud
    ports:
      - '5439:5434'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: